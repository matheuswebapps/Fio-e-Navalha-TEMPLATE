
# Barbearia Fio & Navalha - Site & Admin

Este projeto é um site completo para barbearia com painel administrativo integrado. Ele foi construído para ser fácil de subir no ar e preparado para crescer (conexão com banco de dados).

## 🚀 Como Colocar no Ar (Deploy)

Recomendamos usar o **Netlify** ou **Vercel** por serem gratuitos e simples.

### Opção 1: Via GitHub (Recomendado)
1. Crie um repositório no seu GitHub e suba este código.
2. Acesse [Netlify](https://www.netlify.com) ou [Vercel](https://vercel.com).
3. Clique em "New Site from Git".
4. Selecione seu repositório.
5. Em "Build Settings", use:
   - **Build Command:** `npm run build`
   - **Publish Directory:** `dist`
6. Clique em Deploy.

### Opção 2: Manual (Drag & Drop no Netlify)
1. No seu computador, rode `npm run build`.
2. Uma pasta chamada `dist` será criada.
3. Acesse [Netlify Drop](https://app.netlify.com/drop).
4. Arraste a pasta `dist` para dentro da área pontilhada.
5. Pronto! O site está no ar.

---

## ⚙️ Mudando para Banco de Dados (Supabase)

Atualmente, o site usa o **LocalStorage** (navegador) para salvar os dados do Admin. Isso significa que se você limpar o cache ou abrir em outro PC, as alterações somem.

O projeto já está **PRONTO** para conectar com o Supabase (Banco de Dados Real). Siga os passos:

### 1. Preparação
1. Instale a biblioteca do Supabase:
   ```bash
   npm install @supabase/supabase-js
   ```

### 2. Configurar Supabase
1. Crie uma conta em [Supabase.com](https://supabase.com) e crie um novo projeto.
2. No painel do Supabase, vá em **SQL Editor** e rode o script abaixo para criar as tabelas:

```sql
-- Configurações da Loja
create table shop_settings (
  id int primary key generated by default as identity,
  name text,
  subtitle text,
  phone text,
  instagram text,
  address text,
  map_link text,
  logo_url text,
  hero_image text
);

-- Serviços
create table services (
  id uuid primary key default uuid_generate_v4(),
  name text,
  price numeric,
  duration_minutes int,
  description text,
  icon text,
  active boolean default true
);

-- Cortes Sugeridos
create table cuts (
  id uuid primary key default uuid_generate_v4(),
  name text,
  technical_name text,
  category text,
  image_url text,
  active boolean default true
);

-- Portfólio
create table gallery_photos (
  id uuid primary key default uuid_generate_v4(),
  title text,
  url text,
  active boolean default true
);
```

### 3. Conectar o Código
1. Crie um arquivo `.env` na raiz do projeto:
   ```env
   VITE_SUPABASE_URL=sua_url_do_projeto_supabase
   VITE_SUPABASE_ANON_KEY=sua_chave_anonima_publica
   ```
2. Abra o arquivo `src/config.ts` e mude:
   ```typescript
   export const PROVIDER_MODE = 'supabase';
   ```
3. Abra `src/providers/supabaseProvider.ts`:
   - Descomente as linhas de importação (`import { createClient }...`).
   - Implemente as chamadas reais (o código já tem exemplos comentados).

### 4. Deploy da Atualização
Faça o processo de deploy novamente (`npm run build`). Agora o site usará o banco de dados real e as alterações no Admin serão visíveis para todos os clientes em qualquer dispositivo.
